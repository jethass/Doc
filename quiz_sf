Which of the following functions could be used to break a string into an array?

1 -string_split()
2 -split()
3 -explode()           <true>
4 -preg_match_all()
5 -array_split()
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
In Symfony Security component:

  1- Symfony\Bundle\SecurityBundle\Security\FirewallContext services are publicly created that correspond to each context you create under your security.yml, a context is called an authenticator
  2- Symfony\Bundle\SecurityBundle\Security\FirewallContext services are publicly created that correspond to each context you create under your security.yml, a context is related to each FirewallContext directly but is overall handled by a generic Symfony\Bundle\SecurityBundle\Security\Firewall object
  3- Symfony\Bundle\SecurityBundle\Security\FirewallContext services are privately created that correspond to each context you create under your security.yml, a context is also known as per each firewall
  4- Symfony\Bundle\SecurityBundle\Security\FirewallContext services are synthetically created that correspond to each context you create under your security.yml, each context is liked to an authenticator

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Using Console component, which of these class allows to create custom output styles ?

  1 Symfony\Component\Console\OutputFormatter
  2 Symfony\Component\Console\OutputStyleFormatter
  3 Symfony\Component\Console\OutputFormatterStyle
  4 Symfony\Component\Console\OutputStyle

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Using XML, which of these lines are correct to inject a service?
  1- <argument type="service" id="my.service" />
  2- <service id="my.service" />
  3- <call method="setService"><argument type="service" id="my.service" /></call>
  4- <method setter="setService"><argument type="service" id="my.service" /></method>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
What is the best way to iterate and modify every element of an array using PHP 5?
  1- foreach($array as $key => &$val) { /* ... * / }
  2- while(list($key, $val) = each($array)) { /* ... * / }
  3- You cannot modify an array during iteration.
  4- for($i = 0; $i < count($array); $i++) { /* ... * / }
  5- foreach($array as $key => $val) { /* ... * / }

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
What is the output of the following PHP script?
$a = 1;
$b = 2.5;
$c = 0xFF;
$d = $b + $c;
$e = $d * $b;
$f = ($d + $e) % $a;
print ($f + $e);

  1- 432.75
  2- 643.75
  3- 643
  4- 432
  5- 257

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Which controller/action allows to render a template without a specific controller?
  1- FrameworkBundle:Controller:template
  2- FrameworkBundle:Template:render
  3- FrameworkBundle:Template:action
  4- FrameworkBundle:Template:template  <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
What three special methods can be used to perform special logic in the event a particular accessed method or member variable is not found?
  1-  __destruct()
  2-  __call($method)
  3-  __set($variable, $value)
  4-  __get($variable)
  5- __call($method, $params)

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Using a compiler pass, how do you retrieve a definition of service ?
  1- $container->getDefinition('my.service')
  2- $container->findDefinition('my.service')   <true>
  3- $container->getService('my.service')
  4- $container->retrieveDefinition('my.service')

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
In Twig, which other Twig tag works as the old raw tag, for when you want to display code  and not be parsed?
  1- raw2
  2- noparse
  3- verbatim     <true>
  4- raw
  5- escape with some special parameters
  6- spaceless

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 In Twig, when compiling a node tree how can you change the default compiler on your $twig object
  1 $twig->compileWith($compiler)
  2 $twig->load($compiler)
  3 $twig->compile($compiler)
  4 $twig->setCompiler($compiler)   <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 We can also apply constraints on class getters with ``addPropertyConstraint()``
  1- True  <true>
  2- False


-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Setting a HTTP cookie on the client which is not URL-encoded is done how in PHP 5?
  1- All cookies must be URL encoded
  2- Use the setrawcookie() function   <true>
  3- Setting the $no_encode parameter of setcookie() to a boolean 'true'
  4- Set the cookies.urlencode INI directive to false
  5- Use urldecode() on the return value of setcookie()
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Consider the following script:
$oranges = 10;
$apples = 5;
$string = "I have %d apples and %d oranges";
???????

What could be placed in place of ?????? to output the string:
"I have 5 apples and 10 oranges"

  1- sprintf($string, $oranges, $apples);
  2- printf($string, $apples, $oranges);     <true>
  3- str_format($string, $apples, $oranges);
  4- print($string, $apples, $oranges);
  5- print sprintf($apples, $oranges);

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
How to access the `bar` POST parameter from Request object $request ?
  1- $request->query->get('bar');
  2- $request->post('bar');
  3- {$request->query->all()}['baz'];
  4- $request->request->get('bar');   <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
How to add an extra field `extra` with the form ?
  1- $builder->add('extra', null, ['validation' => false]
  2- $builder->add('extra', hidden, ['mapped' => false])
  3- $builder->add('extra', null, ['mapped' => false])  <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
ESI need to be activated in configuration.
  1- True
  2- False   <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
How to access standard error stream in PHP ?
  1- $stderr = fopen("php://stderr", "w");  <true>
  2- $stderr = fwrite("php://stderr");
  3- $stderr = stderr("w")
  4- $stderr = STDERR;

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
What are existing compiler pass order ?
  1- PassConfig::TYPE_BEFORE_REMOVING
  2- PassConfig::TYPE_AFTER_COMPILING
  3- PassConfig::TYPE_REMOVE
  4- PassConfig::TYPE_BEFORE_OPTIMIZATION
  5- PassConfig::TYPE_OPTIMIZE

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Which of the following is the best way to split a string on the "-=-" pattern?
  1- explode("-=-", $string);
  2- They all are equally proper methods
  3- preg_split("-=-", $string);   <true>
  4- str_split($string, strpos($string, "-=-"))

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Using Form component, which option will allow you to specify groups used for validation?
  1- groups_validator
  2- groups_validation
  3- validation_groups  <true>
  4- validator_groups

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Which one of these Response methods check if cache must be revalidated?
  1- $response->getRevalidated()
  2- $response->mustRevalidate()    <true> 
  3- $response->isRevalidated()
  4- $response->mustBeRevalidated()

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
How to access the `foo` GET parameter from Request object $request ?
  1- $request->query->get('foo');  <true> 
  2- $request->get('foo');
  3- $request->request->get('foo');
  4- {$request->query->all()}['foo'];

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------  
How do you display complete configuration of a bundle ?
  php app/console config:dump AcmeBundle
  php app/console dump:config acme
  php app/console config:dump-reference acme
  php app/console config:dump-reference AcmeBundle  <true> 

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
How to launch Console application in interactive mode?
  1- php app/console --interactive
  2- no one
  3- php app/console -s
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------  
 As dev environment, what is the name of dumped container ?
  1- appDevDebugContainer
  2- appDevDebugProjectServiceContainer
  3- appDevProjectContainer.php
  4- appDevDebugProjectContainer.php

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
in Twig, suppose you have an object person

  1- If person has a property address-main, I can access it with person.getAddressMain()
  2- If person has a property address_main, I better access it with person.['address_main']
  3- a and b are possible
  4- I have to create a special getter for accessing the property above
  5- no, the [] operand works in any situation
  6- get_attribute(person, 'address-main') does it
  7- none of the above, Twig comes with a special function for this

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Using FrameworkBundle configuration, what is the default framework.templating.assets_version_format value?
  1- %%s ? %%s  <true> 
  2- ?%%s
  3- ?%s
  4- %s?%s

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Which method allows to prevent any other Event listeners from being called?
  1 -stopPropagation()   <true> 
  2 - off()
  3 - preventDefault()
  4 - stop()

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  Which function would you use to add an element to the beginning of an array?
   1- array_pop()
   2- array_push()
   3- array_shift()
   4- $array[0] = "value";    <true>
   5- array_unshift()         <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Which of the following is not a valid fopen() access mode:
  1- r+
  2- a
  3- w
  4- x
  5- b                        <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Given the two values below, which of the following possiblities will print 10 foos20 bars?
$var1 = "10 foos";
$var2 = "20 bars";
print ???????;

  1- implode("", array($var1,$var2));  <true>
  2- $var1 . $var2                     <true>
  3- All of the above
  4- $var1 + $var2
  5- None of the above

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Which date type exist?
  1- date   <true>
  2- time
  3- timestamp
  4- datetime  <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
In Kernel class, which method do you need to redefine if you want to have multiple configuration files (config_dev.yml, config_test.yml, ...)?

  1- public function registerConfigurationPath(LoaderInterface $loader)
  2- public function registerContainerConfiguration(LoaderInterface $loader)   <true>
  3- public function registerConfiguration(LoaderInterface $loader)
  4- public function registerConfigurationContainer(LoaderInterface $loader)

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

 How do you bind a constraint to a form field?

  1- Using option 'constraints' in $formBuilder->add()  
  2- Add an annotation in model  <true>
  3- Invoke $formBuilder->setConstraints() method
  4- Passing constraint instance in $this->createFormBuilder()  <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 What is the command to check the syntax of a Twig template?

  1- php app/console twig:lint
  2- php app/console twig:check
  3- php app/console twig:syntax
  4- php app/console twig:validate
  5- php bin/console lint:twig app/Resources/views  <true>
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Using Config component, which of these configuration node types are available?
  string
  array     <true>
  scalar    <true> 
  enum      <true>

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  What is the right command name to load Doctrine fixtures?
  1- php app/console doctrine:load:fixtures
  2- php app/console doctrine:fixtures:load  <true>
  3- php app/console doctrine:fixtures:import
  4- php app/console doctrine:fixtures




 





































